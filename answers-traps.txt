Q1: Which registers contain arguments to functions? For example, which register holds 13 in main's call to printf? 
A1: Conventionally the RISC-V passes its arguments in register a0-a7. Register a2 holds 13 when call printf(13 is the third param).

Q2: Where is the call to function f in the assembly code for main? Where is the call to g? (Hint: the compiler may inline functions.) 
A2: Compiler directly computes the result and directly li 12 in register a1, so in fact there is no call to function f and g in assembly code.

Q3: At what address is the function printf located?
A3: 63e.

Q4: What value is in the register ra just after the jalr to printf in main?
A4: 0x38. By compute 0x38 auipc ra,0x0.

Q5:Run the following code.

	unsigned int i = 0x00646c72;
	printf("H%x Wo%s", 57616, &i);
      

What is the output? Here's an ASCII table that maps bytes to characters.

The output depends on that fact that the RISC-V is little-endian. If the RISC-V were instead big-endian what would you set i to in order to yield the same output? Would you need to change 57616 to a different value?
A5: The output is "He110 World". 57616 in DEX is 0xe110, &i is interpreted as a pointer pointing to a string. 
Because RISC-V is little-endian in memory, so the memory start from &i is 0x72 0x6c 0x64 0x00, that is 'r' 'l' 'd' '\0'.
If RISC-V is big-endian, to yield the same output, we should set i=0x726c6400.
Don't need to change 57616.

Q6:In the following code, what is going to be printed after 'y='? (note: the answer is not a specific value.) Why does this happen?

	printf("x=%d y=%d", 3);

A6: It is an undefined behavior in C. The value of y is not fixed, it may depends on the value in register a2.
